package com.example.promptdb.api.dto;

/** Mirrors the role enum shared by MemberResponse, GroupMembershipResponse, AddMemberRequest and ChangeMemberRoleRequest in the OpenAPI spec. */
public enum Role {
    ADMIN,
    GROUP_LEAD,
    MEMBER
}
